# Experimental no-eBPF dual-stack values for the NetworkPolicy phase.
# Layer on top of kind-no-ebpf-dual.yaml to enable the nftables KNP backend.
noEBPF:
  networkPolicy:
    enabled: true
policyEnforcementMode: never
